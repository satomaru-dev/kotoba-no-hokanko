# ことばの保管庫 PROJECT CONTEXT

最終確認日: 2026-08-29
対象ブランチ: `main`

この文書は、ノートPCと自宅のデスクトップPC、または別のCodexタスク間で開発を引き継ぐための基準情報である。実装を変更した場合は、コードと食い違わないよう必要に応じて更新する。

## プロジェクトの目的

思いついたメモ、アイデア、悩み、判断材料を、整理方法や使用媒体を固定せず残せる個人用の記憶基盤を作る。

保存した言葉を一覧へ蓄積するだけでなく、新しい言葉を残した時や検索した時に、関係する過去の記録を再提示し、以前の自分の考えと再会できるようにする。スマホのPWA「ことばの保管庫」と、ChatGPT／Codexから呼び出す共通記憶検索を同じ記憶基盤へ接続する構成である。

## アプリのコンセプト

> 思いついた言葉を投げるだけで、自分の言葉と温度を保ったまま蓄積され、必要な時に過去のアイデアと再会できる場所

中心にある考え方は次のとおり。

- 整理することより、残すことを簡単にする。
- 原文を勝手に要約・言い換えしない。
- 本人の言葉遣い、感情、迷い、勢い、比喩を保つ。
- AIの解釈と本人の原文を混ぜない。
- 本人に定期レビューを要求せず、現在との関連によって過去を戻す。
- 一般的なタスク管理、フォルダ階層、複雑なステータス管理へ寄せない。
- 「あとでやる」はタスクをゼロにする仕組みではなく、先延ばしを許しながら現在の自分に合わせて配置する「フォーメーション」として扱う。

## 想定している利用者

- 主な利用者はプロジェクト所有者本人。
- AndroidスマートフォンのMicrosoft Edge等から、ホーム画面へインストールしたPWAとして使う。
- PCブラウザとPCローカル版からも利用する。
- プライベート用と仕事用のメールアカウントを分ける案がある。実際の許可メール一覧とアカウント運用はリポジトリ内では確認できないため未確定。
- 多人数向けサービスとして公開する計画は未確定。

## 現在できていること

- 大きな入力欄へ文章を書き、`残す`を押して原文のまま保存する。
- 保存直後に、関連する過去の記録を近い順で最大10件表示する。
- 関連記録へ`近い／違う`の評価を付ける。
- 最近のメモを見る、自然文で検索する、編集履歴と最初の原文を見る。
- メモをゴミ箱へ移動し、復元する。クラウド側には30日経過後の削除処理がある。
- オフライン時の新規メモをIndexedDBへ保持し、通信復旧後に再送する。
- 保存した言葉を、3種類の置き場所へ本人の直感で振り分ける。
  - `あとでやる`
  - `しばらく見えるところに置いておきたい`
  - `今の自分にとって結構重要な気づき`
- `あとでやる`項目を完了・中止・延期し、解決済み項目を`これまで`で見る。
- `あとでやる`カードを約0.5秒長押しし、ドラッグで好きな位置へ並べ替える。
- `まだやらない`を押した時に、その時やらない理由を本人の自由記入で残す。
- 延期理由を日時・延期時点のメモ本文とともに蓄積し、CSVで書き出す。
- 検索履歴と頻出検索語を表示する。
- 条件付きで、過去の記録へ今の自分の言葉を返す対話、リマインダー、最初の一歩、URL、ルーレットを使う。
- PCローカル版では、`あとでやる`項目へ作業フォルダを結び付けて開く。
- Codex／ChatGPT向けMCPの`recall_related`から、強く関連する記憶を最大3件取得する。

## 現在の画面構成

### 起動・認証

- 起動中は12fpsのリスのアニメーションを表示する。
- 動きを減らす端末設定では静止アイコンへ切り替える。
- クラウド版の未ログイン時は、メールのマジックリンクによるログイン画面を表示する。

### 下部4タブ

現在の並び順は次のとおり。

1. `書く`
2. `あとでやる`
3. `最近`
4. `さがす`

### 書く

- ChatGPT風の、本文入力と保存操作が一体になった入力画面。
- 未入力時は`残す`ボタンを表示しない。
- 保存後はキーボードを閉じ、入力欄をリセットする。
- 保存結果、3種類の置き場所、関連記憶最大10件を表示する。

### あとでやる

- 有効な`do_later`項目だけを表示する。
- 操作は`やってあげた。`、`まだやらない`、`やっぱりやめる`。
- 長押しドラッグによる手動並べ替えと、ドラッグ中の自動スクロールがある。
- `まだやらない`では自由記入画面を出し、1〜1000文字の理由を必須で残してから一覧末尾へ移動する。
- 同じ項目を複数回延期した場合も理由を上書きせず、延期時点のメモ本文と日時を履歴として追加する。
- 毎日繰り返す項目の`今日はやらない`は理由入力を出さず、当日だけ非表示にする。
- `これまで`を展開すると、蓄積した延期理由をCSVで書き出せる。
- 既存の`気持ちが重い`データは互換性のため保持するが、新しい延期では追加しない。
- 解決済みの項目は折りたたみ式の`これまで`へ表示する。
- 開始補助βが有効なら、最初の一歩、URL、ルーレットを表示する。

### 最近

- 有効な`do_later`項目は表示しない。
- 空でないグループだけを次の順で表示する。
  1. `しばらく見えるところに置いておきたい`
  2. `今の自分にとって結構重要な気づき`
  3. `その他のアイデア`
- 右上のゴミ箱アイコンで、最近一覧とゴミ箱を切り替える。
- 一覧はタイトルを表示せず、日付と本文だけを表示する。

### さがす

- ピル型の検索欄へ自然文を入力し、Enter／検索キーで検索する。
- 検索前は`最近さがした言葉`と`よく出てくる言葉`を最大8件ずつ表示する。
- 検索結果は関連記憶カードとして最大10件表示する。

### 詳細・対話

- 詳細では現在の本文、最初に保存した原文、編集履歴を表示する。
- 表示上のタイトル欄は廃止済みで、本文だけを主表示にする。内部データの互換性のためタイトル項目自体は残っている。
- 編集、ゴミ箱移動、復元、置き場所変更ができる。
- 対話βが有効なら、過去の記録へ本人の原文のまま返事を追加し、時系列で表示する。

## 現在の機能

### 記録と編集

- スマホ側で一意の`client_id`を発行し、二重保存を防止する。
- `original_text`を不変の原文として保持し、`current_text`と編集履歴を別に保存する。
- 編集・ゴミ箱・復元のAPI成功後は、返却された確定データを画面のStateへ即時反映する。
- 古い一覧取得が後から完了して新しい表示を上書きしないための世代管理がある。

### 関連記憶と検索

- スマホ保存後と自然文検索では、関連記憶を最大10件表示する。
- MCPの`recall_related(query, max_results)`は最大3件を返す。
- 関係ラベルは`同じ悩み・発想`、`組み合わせ可能`、`以前と変化`、`試したが残らなかった`。
- 過去の記録は現在の真実と断定せず、その時点で記録された考えとして扱う。
- 検索は語句の重なりとハッシュベクトル等を組み合わせ、重複やリンク集らしい断片を調整する。

### あとでやる・重要度

- `attention_level`は`do_later | keep_in_mind | important_insight`。
- `status`は`active | done | abandoned`。
- 新規／再登録した`do_later`項目は先頭へ入り、手動順を保持する。
- `まだやらない`は自由記入理由の保存成功後に対象を現在の一覧から取り除き、配列末尾へ追加する。保存後も同じ順番を維持する。
- 延期理由は追記専用の履歴として扱い、クラウドでは理由と延期時点本文を暗号化する。
- 手動並べ替えは`@dnd-kit/core`と`@dnd-kit/sortable`を使用し、タッチ操作は500msの長押しで開始する。
- `heavy_marked`は旧仕様との互換性のため保持する。自由記入による新しい延期時は`false`へ戻す。

### オフライン・PWA

- IndexedDBに未送信メモと未送信リマインダーを保持する。
- Service Workerがアプリ本体、アイコン、リスのアニメーションをキャッシュする。
- PWAのホーム画面アイコン、起動表示、セーフエリア対応がある。
- 音声入力とアプリアイコン長押しの音声ショートカットは削除済み。

### プライバシー・認証

- Supabase Authのユーザー単位でデータを分離する。
- クラウドDBでは本文、タイトル、出典をAES-256-GCMで暗号化する。
- 検索語はHMAC-SHA-256等によるブラインド索引を使用し、平文ログを残さない方針。
- `.env`、パスワード、認証情報、顧客、契約、被保険者、金融、口座、クレジット、CSV等を索引から除外するコードがある。
- 暗号鍵とサービス用トークンはSupabase Edge FunctionのSecretへ置き、PWAやGitへ入れない。

## 実装済みの内容

- フロントエンド: React 19、TypeScript、Vite、PWA。
- スマホUI: `web/src/App.tsx`と`web/src/styles.css`。
- ローカルAPI: Express 5、`src/http.ts`、JSONファイル保存。
- クラウドAPI: Supabase Edge Function `memory-api`。
- クラウドDB: Supabase Postgres。RLS、暗号化列、検索索引、対話、リマインダー、あとでやる、検索履歴のマイグレーションがある。
- 共通記憶: MCP Server、stdio起動、`recall_related` Skill。
- 外部記録アダプター: Obsidian、Notion、Google Drive、コネクタースナップショット。
- PC同期: 単発同期、常駐同期、Windowsタスク登録用スクリプト。
- PCローカル補助: 作業フォルダの選択・作成・ファイル追加・起動。
- 静的配信: `main`へのpushでGitHub Actionsがビルドし、GitHub Pagesへ公開する。
- 本番フロントエンド: `https://satomaru-dev.github.io/kotoba-no-hokanko/`。
- テスト: 保存・検索・MCP・オフライン・メモState・作業フォルダ等のVitest。
- 基準コマンド: `pnpm typecheck`、`pnpm test`、`pnpm build`。

## 現在の課題

- 2026-08-29の作業開始時点で、ローカル`main`と`origin/main`は同期している。
- `docs/icon-concepts/ことばの保管庫/`は未追跡であり、Gitへ追加するか別保管にするか未確定。このままでは別PCへGit経由で移らない。
- `docs/current-baseline.md`には古いタブ順、古い操作文言、日時リマインダー中心の説明が一部残る。この`PROJECT_CONTEXT.md`と実装を優先し、必要時に基準版文書も更新する。
- 関連記憶の精度は実利用で継続評価する必要がある。代表質問20件の最終評価結果はリポジトリ内で確認できず未確定。
- 対話β、リマインダーβ、開始補助βの長期的な採用判断は未確定。
- Supabase Freeの休止、Push通知のCron、本番通知の実機での継続動作は、この確認だけでは確定できない。

## これまでに決めた設計方針

- 専用の管理画面を増やさず、日常の入口はスマホPWA、ChatGPT、Codexとする。
- 初版は本人一人を中心に、月額0円を優先する。有料AI APIへの自動移行はしない。
- 原本は各保管元に残し、索引では許可範囲だけを扱う。
- 原文を不変で保存し、手直しは履歴として別に残す。
- タイトルはユーザー画面に表示せず、本文だけで扱う。
- フォルダ、タグ、期限、優先度、チェックリスト、未完了件数による催促を基本機能へ加えない。
- `あとでやる`は行動を強制する場所ではなく、主体的な先延ばしを可能にする場所とする。
- `やっぱりやめる`も一つの解決として履歴へ残す。
- `まだやらない`は本人がその時の理由を自分の言葉で残して主体的に選ぶ。理由は後から分類できるよう原文のまま蓄積する。
- タスクの順番はAIが優先度判定せず、本人が長押しドラッグでフォーメーションを変える。
- 過去との対話はAIによる変化の断定ではなく、各時点の本人の原文を時系列で結ぶ。
- 弱い関連記憶は無理に出さない方針から始まったが、現在のPWAでは関連候補を近い順に最大10件まで見られる設計。
- 音声入力は採用しない。ネイティブアプリ化は機能と使い勝手を整えた後に再検討する。
- 新機能は、既存機能を壊さないβ追加か既存仕様変更かを先に区別する。

## デザイン上の希望

- 温かく静かな保管庫の印象。整理や実行を迫るタスク管理アプリの緊張感を出さない。
- 基本色:
  - ページ背景 `#FAF7F1`
  - カード `#FFFFFF`
  - 主テキスト `#1F1A15`
  - 副テキスト `#7A6E60`
  - 罫線 `#EDE5D8`
  - Primary `#E07B21`
  - Primary文字 `#B65510`
  - Primary淡色 `#FDF2E3`
- リスのキャラクター名は`うっかリス`案があり、正式確定かは未確定。アプリアイコンは琥珀色系のリス。
- ヘッダーは56px、小さなリスアイコンと画面名を1行で表示する。
- 下部ナビは4タブ、SVGアイコン、48px以上のタップ領域、アクティブ時の淡いオレンジ背景を使う。
- 本文とカードはゴシック、見出し・空状態・入力プレースホルダー等は明朝を使う。
- 一覧は本文を主表示にし、タイトルとの重複を作らない。
- 日付は`きょう`、`きのう`等の相対表示を使う。
- 動くリスは起動中、保存完了、ボタン操作後など意味のある短い場面に限定し、常時動かさない。
- スマホでスクロールやキーボード操作を増やさず、入力から保存までの摩擦を小さくする。

## 今後やること

- 最新のローカルコミットと今回の引き継ぎ文書を、確認後に`origin/main`へpushし、両PCから取得できる状態にする。
- 2台目のPCではリポジトリをcloneし、Node.js 20以上とpnpm 11.9.0を用意して`pnpm install --frozen-lockfile`を実行する。
- `.env`と`web/.env.local`はGitで同期せず、パスワード管理アプリ等の安全な方法で各PCへ設定する。
- 作業開始時は`git fetch`、`git status`、`git pull --ff-only`を行い、終了時はテスト後にコミット・pushする運用を基本にする。
- 関連記憶の提示精度を実利用と`近い／違う`で評価する。
- 対話β、通知β、開始補助βを実利用し、残すものと外すものを判断する。
- Notion、Google Drive、Obsidianの許可範囲と自動同期の本番運用を確認する。
- ChatGPTやGeminiへ話したアイデアを二重入力せず保管庫へ残す方法を検討する。
- 必要性が明確になった段階でネイティブアプリ化を再検討する。

## 変更してはいけないこと

- コンセプト文を、明確な本人の判断なしに変更しない。
- 保存した原文、句読点、改行、言葉遣いを自動で要約・言い換え・上書きしない。
- 本文中心のUIへ、タイトル入力やタイトル重複表示を勝手に戻さない。
- 下部4タブと現在の並び順を、依頼なしに変更しない。
- `あとでやる`を期限・優先度・チェックリスト中心の一般的なタスク管理へ変えない。
- `やってあげた。`、`まだやらない`、`やっぱりやめる`の意味を、依頼なしに変えない。
- `まだやらない`の理由を本人の原文で残し、理由を保存した後は確実に延期できる設計を維持する。
- 手動並べ替え、新規項目を先頭へ置く動作、延期項目を末尾へ置く動作を無断で変えない。
- 顧客、契約、金融、認証情報、`.env`等の除外規則を弱めない。
- RLS、暗号化、本人認証を回避しない。秘密情報をコード、ログ、Gitへ出さない。
- 音声入力を再追加しない。
- 有料APIや自動課金を、本人の明示的な承認なしに追加しない。
- 未追跡の素材や利用者の変更を、確認せず削除・移動・コミットしない。

## 未確定の事項

- `うっかリス`が正式名称かどうか。
- プライベート用／仕事用メールを、同一プロジェクト内の別ユーザーとして恒久運用するか。
- GitHub Actions上の各βフラグの現在の登録値。未登録時はワークフローの既定値`true`になる。
- 対話β、リマインダーβ、開始補助βを正式機能にするか、変更または削除するか。
- Push通知用CronとWeb Pushが現在すべての対象端末で稼働しているか。
- Notion、Google Drive、Obsidianの現在の同期対象、同期件数、30分ごとの常駐同期が稼働中か。
- ChatGPTカスタムアプリが現在も共通MCPへ接続されているか。Geminiとの連携方法。
- 関連検索が代表質問20件中16件以上という合格条件を達成したか。
- Goodnotes、紙、Google Keepを将来取り込むか。
- ネイティブAndroidアプリへ移行する時期。
- `docs/icon-concepts/ことばの保管庫/`の未追跡素材をGit管理へ含めるか。

## PC間で作業を引き継ぐ時の手順

### 作業開始前

```powershell
git fetch origin
git status -sb
git pull --ff-only origin main
pnpm install --frozen-lockfile
```

- `PROJECT_CONTEXT.md`を読む。
- `.env`と`web/.env.local`が必要なら、安全な保管場所から各PCへ設定する。
- 未コミット変更がある場合は、誰の何の変更か確認してから作業する。

### 作業終了前

```powershell
pnpm typecheck
pnpm test
pnpm build
git status
```

- 変更内容をコミットし、別PCへ移る前に`main`へpushする。
- Supabaseを変更した場合は、マイグレーション／Edge Functionのローカルファイルと本番デプロイ状態を一致させる。
- 仕様や既知の課題が変わった場合は、この文書を更新する。

## 主要ファイル案内

- `web/src/App.tsx`: スマホ／Web UIと主要な画面State。
- `web/src/styles.css`: 現在のデザイン、レスポンシブ、操作フィードバック。
- `web/src/api.ts`: PWAからローカル／Supabase APIを呼ぶ窓口。
- `web/src/offline.ts`: IndexedDBの未送信キュー。
- `web/src/types.ts`: PWA側の型。
- `src/http.ts`: PCローカルAPI。
- `src/capture-store.ts`: PCローカル版のメモ、あとでやる、検索履歴保存。
- `src/mcp.ts`: `recall_related` MCPツール。
- `src/search.ts`: ローカル共通記憶の再順位付けと関係ラベル。
- `src/security.ts`: 索引対象の拒否規則と認証補助。
- `src/adapters/`: Obsidian、Notion、Google Drive、スナップショットの取り込み。
- `supabase/functions/memory-api/index.ts`: 本番クラウドAPI。
- `supabase/migrations/`: 本番DB変更履歴。
- `.github/workflows/pages.yml`: GitHub Pagesへの自動公開。
- `plugin/contextual-memory/`: Codex向けの関連記憶Skill。
- `docs/`: 製品コンセプト、基準版、公開手順、デザイン資料。
- `.env.example`: 必要な環境変数名の見本。秘密値は入れない。
