# Codex作業ルール

このファイルのルールは、プロジェクト全体に適用する。

## 作業開始時

- 最初に `PROJECT_CONTEXT.md` を読み、目的・現行仕様・未確定事項を確認する。
- `git status`、現在のブランチ、`origin/main` との差分を確認する。
- 既存の未コミット変更や未追跡ファイルは利用者の作業として扱い、依頼と無関係な内容を変更・削除・コミットしない。
- 実装を始める前に、関連するコード、テスト、既存ドキュメントを確認する。

## 変更時の原則

- 既存の機能、デザイン、文言、データ形式を勝手に大きく変更しない。
- 依頼された範囲だけを変更し、関係のない整理やリファクタリングを混ぜない。
- 不明点や複数の解釈があり、結果が大きく変わる場合は推測で決めず利用者へ質問する。
- 原文保存、暗号化、RLS、除外規則、オフライン保存など、データ保護に関わる仕組みを弱めない。
- `.env`、APIキー、暗号鍵、トークン、個人情報をリポジトリへ追加しない。
- アプリ本体を変更した場合は、原則として `pnpm typecheck`、`pnpm test`、`pnpm build`を実行する。
- SupabaseのDBやEdge Functionを変更する場合は、ローカルのマイグレーション／関数コードと本番状態を一致させる。

## 作業終了時

- 変更したファイル名と、それぞれの変更内容を最後に説明する。
- 実行した確認・テストと、その結果を報告する。
- push、デプロイ、本番DB変更を行ったかどうかを明確にする。
- 仕様、運用、重要な判断、既知の課題が変わった場合は、必要に応じて `PROJECT_CONTEXT.md` を更新する。
- 判断できなかった事項は、推測で埋めず「未確定」として残す。
